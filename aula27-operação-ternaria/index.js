// ? :
const userPonts = 900;
const userLevel = userPonts >= 1000 ? "gold user" : "silver user";

const userColor = null
const defaultColor = userColor || 'black'

console.log(userLevel, defaultColor)


/* 
if (userponts >= 100) {
    console.log('gold user')
} else {
    console.log('silver user')
}
*/